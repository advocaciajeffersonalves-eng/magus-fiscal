# Chaves de API — NUNCA versionar
.env
*.env

# Banco de dados — contém dados de usuários (nome, email, telefone).
# NUNCA versionar. O arquivo continua normal na pasta; só não vai pro Git/GitHub.
*.db
usuarios.db

# Cópias de segurança do banco — contêm dados de usuários, nunca versionar
backups/

# Ambiente virtual Python
venv/
.venv/
env/

# Cache Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Streamlit cache local
.streamlit/secrets.toml

# macOS
.DS_Store
.AppleDouble

# Arquivos temporários e de teste
teste_*.csv
*.log

# === Banco de contratos — dados sigilosos de clientes (NUNCA versionar) ===
templates/_originais
templates/_originais/
templates/_trabalho/
templates/**/_originais

# Rascunhos de templates (podem conter resíduos até revisão)
templates/_rascunhos/

# Templates gerados pelo modelo local (rascunhos até curadoria)
templates/_gerados/
templates/_doutrina/

# Backups — nunca versionar (podem conter segredos)
.env.*
*.bak-*
*.bak
